import { NotificationsService } from './notifications.service';

describe('NotificationsService', () => {
  it('should create an instance', () => {
    expect(new NotificationsService()).toBeTruthy();
  });
});
