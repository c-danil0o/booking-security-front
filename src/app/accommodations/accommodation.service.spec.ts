import { AccommodationService } from './accommodation.service';

describe('AccommodationService', () => {
  it('should create an instance', () => {
    expect(new AccommodationService()).toBeTruthy();
  });
});
