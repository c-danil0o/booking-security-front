export class Address {
    street: string;
    city: string;
    number: string;

    constructor(street: string, city: string, number: string) {
        this.street = street;
        this.city = city;
        this.number = number;
    }
}
